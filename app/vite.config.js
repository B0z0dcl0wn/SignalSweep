// SPDX-License-Identifier: GPL-3.0-or-later
// Relative base so the built app works both at a GitHub Pages subpath
// (/SignalSweep/app/) and inside the Capacitor Android WebView.
export default { base: './' };
